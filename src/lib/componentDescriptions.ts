export const COMPONENT_DESCRIPTIONS: Record<string, string> = {
  homeHeader: "The bar across the very top of the home screen — the app logo, the current screen title (Chats / Communities / Profile), the search button, and (on the Communities tab) the + create button. Users might call it: the top bar, the header, the title bar, the bar at the top.",
  homeSearch: "The search input bar that drops down under the home header when the search button is tapped. Users might call it: search box, search field, the find bar.",
  bottomNav: "The bottom navigation bar with the Chats, Communities, and Profile tabs. Users might call it: the bottom bar, the tab bar, the navigation, the bottom tabs, the navbar.",
  contactList: "The scrollable list of chat rows/tiles on the home screen AND the full appearance of each individual row — the person's avatar, the green online ring/indicator around the avatar, the name, the last-message preview, the time, and the unread badge. This is the ONLY component that renders chat rows, so EVERY per-row change (online indicators, badges showing next to online users, avatar styling, row layout) is edited here. Also handles the list container, grouping, and tabs. Users might call it: the chat list, conversations, the inbox, the list of chats, the tiles, a chat row, a conversation tile.",
  bottomSheet: "The slide-up panel/sheet at the bottom (e.g. attachment options). Users might call it: the popup, the sheet, the bottom popup, the drawer.",
  chatScreen: "The overall layout of an open conversation — the container holding the messages area and arranging everything between the header and the composer. Users might call it: the chat, the conversation screen, the messages area, the chat background.",
  messageBubble: "A single message bubble in a conversation — the rounded box holding one message's text and time. Sent vs received styling. Users might call it: the bubble, the message, the text bubble, the chat bubble.",
  dateSeparator: "The small centered pill between messages that shows the date (Today, Yesterday, or a date). Separates messages from different days. Users might call it: the date label, the day divider, the date pill, the divider in the middle.",
  composerBar: "The message input bar at the bottom of a conversation — the area containing the text box where you type, the attach/plus button, and the send button. It sits below the messages. Users might call it: the composer, the input bar, the bottom bar, the typing area, the message box, 'the black box at the bottom', the bar behind the keyboard. Its background color, transparency, border, and whether messages scroll behind it can all be changed.",
  backButton: "The back arrow at the top-left of a chat that returns to the chat list. Users might call it: back arrow, back button, the arrow, return button.",
  profileImage: "The contact's profile photo (or initials circle) shown in the chat header at the top. Users might call it: profile pic, avatar, the photo, display picture, dp.",
  chatName: "The contact's name shown at the top of the chat. Users might call it: the name, contact name, title, who you're talking to.",
  onlineStatus: "The small online/offline text under the name in the chat header. Users might call it: online status, last seen, the status, the green text.",
  attachButton: "The attach / plus (+) button in the message input bar, used to add photos or files. Users might call it: plus button, attach button, the + , add button, paperclip.",
  sendButton: "The send button in the message input bar that sends the typed message. Users might call it: send button, the arrow, submit, the send arrow.",
  emptyState: "The placeholder shown on the home screen when you have no chats yet — the centered 'No chats yet' message with the 'Search for friends and start chatting' hint. Users might call it: empty screen, no chats message, the blank state, the welcome message, nothing-here screen.",
  messageStatus: "The checkmark(s) next to the time on a sent message — single check for sent, double for delivered, blue double for read. Icons, colors, and position can all be changed. Users might call it: the ticks, the checkmarks, the read receipts, the double tick.",
  typingIndicator: "Shows 'typing...' when the other person is composing a message. Sits in the header by default but can be moved anywhere in the chat and restyled with animation. Users might call it: typing indicator, the typing dots, 'is typing', the typing bubble.",
  replyPreview: "The 'Replying to...' bar that appears above the message input when a message has been selected to quote, with an X to cancel. Layout, colors, animation, and what triggers it (tap, swipe, long-press) can all be changed. Users might call it: reply preview, the replying-to bar, the quote bar above the input.",
  replyQuote: "The small quoted snippet shown inside a message that's itself a reply — tapping it scrolls to and briefly highlights the original message it quotes. Layout, colors, and what tapping it does can all be changed. Users might call it: the quote inside the bubble, the quoted message, the reply preview on the message.",
  messageReactions: "The small emoji+count pills shown under a message once people have reacted to it — tapping a pill adds your own reaction or removes it if it's already yours. Colors, layout (e.g. overlapping the bubble's corner), and animation can all be changed. Users might call it: the reactions, the emoji under the message, the reaction pills.",
  reactionPicker: "The small tappable icon on each message that opens a row of emoji to react with. Which emoji are offered, how it opens (tap, long-press, swipe), and its look can all be changed. Users might call it: the react button, the emoji picker, the smiley icon on the message.",
  profileScreen: "The 'My Profile' screen where the signed-in user views and edits their own name, about/bio text, and profile photo, plus logs out. Layout, fields shown, and styling can all be changed. Users might call it: my profile, account settings, edit profile.",
  contactProfileScreen: "The read-only profile view shown when tapping a contact's photo in a chat — shows their avatar, name, @username, online status, and bio. Layout and styling can all be changed. Users might call it: contact info, the other person's profile, user profile screen.",
  communityMessageBubble: "A single message bubble inside a community/group chat — shows sender avatar and name on incoming messages, right-aligns own messages. Style, layout, and what info is shown can all be changed.",
  communityListScreen: "The communities browse/list screen showing public communities and ones the user has joined, with join and open buttons and a create (+) button. Layout, card design, and filtering can all be changed.",
  createCommunityScreen: "The form screen for creating a new community — name, description, and type (public/protected/private) fields. Fields, type options, and styling can all be changed.",
  communityChatScreen: "The full chat screen inside a community — header with name and member count, scrollable message list, and a text composer at the bottom. Join/Leave button, layout, and styling can all be changed.",
  communityProfileScreen: "The community info/profile page showing the community avatar, name, type, description, member count, and members list — tappable via the avatar in the community chat header. Owners can edit the community photo. Layout, sections, and styling can all be changed.",
  settingsScreen: "The Settings screen reached from the profile tab — sections for Notifications (push toggle), Privacy (blocked users list in a sheet), and Account (change password, log out, delete account). Layout, sections, toggles, sheets, and styling can all be changed. Users might call it: settings, preferences, the settings page, account settings.",
}
