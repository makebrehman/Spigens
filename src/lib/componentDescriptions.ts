export const COMPONENT_DESCRIPTIONS: Record<string, string> = {
  topAppBar: "The bar across the very top of the home screen. Contains the app title/'messages' heading and the top icons (menu, search, compose). Users might call it: the top bar, the header, the title bar, the bar at the top.",
  searchBar: "The search input where users type to find people. Users might call it: search box, search field, the find bar.",
  contactList: "The scrollable list of chat tiles on the home screen — each row showing a person's avatar, name, last message, and time. Handles the whole list container, the tiles, grouping, tabs. Users might call it: the chat list, conversations, the inbox, the list of chats, the tiles.",
  bottomSheet: "The slide-up panel/sheet at the bottom (e.g. attachment options). Users might call it: the popup, the sheet, the bottom popup, the drawer.",
  chatScreen: "The overall layout of an open conversation — the container holding the messages area and arranging everything between the header and the composer. Users might call it: the chat, the conversation screen, the messages area, the chat background.",
  messageBubble: "A single message bubble in a conversation — the rounded box holding one message's text and time. Sent vs received styling. Users might call it: the bubble, the message, the text bubble, the chat bubble.",
  dateSeparator: "The small centered pill between messages that shows the date (Today, Yesterday, or a date). Separates messages from different days. Users might call it: the date label, the day divider, the date pill, the divider in the middle.",
  composerBar: "The message input bar at the bottom of a conversation — the area containing the text box where you type, the attach/plus button, and the send button. It sits below the messages. Users might call it: the composer, the input bar, the bottom bar, the typing area, the message box, 'the black box at the bottom', the bar behind the keyboard. Its background color, transparency, border, and whether messages scroll behind it can all be changed.",
  chatTile: "A single chat row in the contact list. Users might call it: chat row, conversation tile.",
  backButton: "The back arrow at the top-left of a chat that returns to the chat list. Users might call it: back arrow, back button, the arrow, return button.",
  profileImage: "The contact's profile photo (or initials circle) shown in the chat header at the top. Users might call it: profile pic, avatar, the photo, display picture, dp.",
  chatName: "The contact's name shown at the top of the chat. Users might call it: the name, contact name, title, who you're talking to.",
  onlineStatus: "The small online/offline text under the name in the chat header. Users might call it: online status, last seen, the status, the green text.",
  attachButton: "The attach / plus (+) button in the message input bar, used to add photos or files. Users might call it: plus button, attach button, the + , add button, paperclip.",
  sendButton: "The send button in the message input bar that sends the typed message. Users might call it: send button, the arrow, submit, the send arrow.",
  emptyState: "The placeholder shown on the home screen when you have no chats yet — the centered 'No chats yet' message with the 'Search for friends and start chatting' hint. Users might call it: empty screen, no chats message, the blank state, the welcome message, nothing-here screen."
}
